"use client"

// Stripe functionality is disabled - checkout redirect component disabled
console.log(
  "Checkout redirect component disabled - Stripe functionality removed"
)

export function CheckoutRedirect() {
  // Component disabled since Stripe functionality is removed
  return null
}
